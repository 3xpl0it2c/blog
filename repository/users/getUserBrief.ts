/*
Fetch a user's name, avatar photo and bio.
That's it.
This is here because when a user views an article,
He does not need to see the author's other articles or comments.
*/

const main = () => async () => {};

export const getUserBrief = main;
